{
  config,
  lib,
  pkgs,
  ...
}:
with lib; {
  options.M.isNixos = mkEnableOption "Enable support for Nixos systems";

  (mkIf config.M.isNixos imports = custom.scanPath {path = ./.; });

  config = config.M.isNixos {
	  environment.systemPackages = with pkgs; [];
  };
}
